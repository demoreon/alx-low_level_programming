#include <stdio.h>

/**
*main - prints the alphabet in Alphabet Soup
*
*Return: ALways 0
*/
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		if (a != q || e)
			putchar(a);
	putchar('\n');
	return (0);
}
